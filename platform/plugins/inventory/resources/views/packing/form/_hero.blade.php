<div class="packing-glassline-page">
    <section class="packing-glassline-hero">
        <div>
            <div class="packing-glassline-eyebrow">Packing</div>
            <h1>{{ $formTitle }}</h1>
            <p>{{ $formSubtitle }}</p>
        </div>
        <div class="packing-glassline-hero__meta">
            <span><strong>{{ number_format($exportCount) }}</strong> phiếu xuất khả dụng</span>
            <span><strong>{{ number_format($warehouseCount) }}</strong> kho khả dụng</span>
        </div>
    </section>

    <section class="packing-glassline-card">
        <div class="packing-glassline-section-heading">
            <div>
                <span>Thông tin phiếu</span>
                <strong>Thiết lập chứng từ đóng gói</strong>
            </div>
            <small>Các tổng số kiện, trọng lượng và số lượng sẽ tự tính từ danh sách kiện bên dưới.</small>
        </div>
